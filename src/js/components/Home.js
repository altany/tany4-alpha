import React from 'react';
import {Link} from 'react-router';

/**
 * A counter button: tap the button to increase the count.
 */
class Counter extends React.Component {
  render() {
    return (
      <Link to="page">Visit a new page</Link>
    );
  }
}
export default Counter;
